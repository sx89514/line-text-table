---
name: line-text-table
version: 0.1.1
description: >-
  把資料排成能直接貼到 LINE 手機聊天室、不會歪也不會換行的純文字框線表格（┌┬┐│├┼┤└┴┘），
  例如空檔時間表、報價比較、簡單對照表。用戶說「用框線符號排」「做成 LINE 可以傳的表格」
  「排我的空檔時間」「傳到聊天室的表格」時使用。
  不適用：要 Markdown 表格、要貼到 Slack／Notion／Email 等支援 Markdown 或 HTML 的地方、
  要做成圖片或 Excel 檔。
user-invocable: true
last-updated: 2026-09-14
---

# LINE 框線表格

LINE 手機版不支援 Markdown，也不是等寬字型。直接照一般 ASCII 表格排，傳出去一定歪或被換行。
這份 Skill 記錄的是 2026-09-14 在 LINE Android 上實際傳送、截圖驗證過的排版規則。

## LINE 排版事實（實測）

| 事實 | 狀態 |
| :-- | :-- |
| 框線字元、中文字、全形英數（`１５` `Ｏ` `Ｘ`）、全形空白 `　`、全形斜線 `／`、`●` 全都一樣寬 → 以「格」計算，一字一格 | ✅ 實測 |
| 半形字（`15`、`O`、半形空白）比全形窄、而且不成比例 → 格子裡一律換成全形 | ✅ 實測會歪 |
| 總寬 12 格放得下（9／10／12 格都實測過）；15 格會被切成好幾行。上限先抓 **13 格**（13、14 格還沒實測） | ⚠️ 部分實測 |
| 行與行之間有行距，直線看起來斷斷續續，這是正常，不用修 | ✅ 實測 |
| `○` `✕` `★`、emoji 的寬度 | ❓ 未實測，避免使用 |

實測到新符號或新寬度上限時，回寫這張表，並同步修改 `scripts/check_table.py` 的 `VERIFIED_SYMBOLS` 或 `--max-width` 預設值。

## 必要輸入

- 表格內容（用戶通常用口語講，例如「週二週三上下午都可以，週四只有上午」）。
- 有日期時：是哪一週。沒講就用「從今天起最近的那幾天」，並在回覆裡寫出你用的日期範圍。
- **星期幾不要心算**，用指令算：`python -c "import datetime as d; print(d.date(2026,9,15).isoweekday())"`（1＝週一）。

## 排版流程

1. **算寬度**：總寬＝各欄字數加總＋直線數（欄數＋1）。同一欄每一格字數必須相同，不足用全形空白 `　` 補。
2. **選方向**：行列兩種擺法都算，選總寬比較窄的那個。
3. **超過 13 格，依序縮減，縮到放得下就停：**
   1. 行列轉置。
   2. 縮短文字：`週二`→`二`、`上午`→`上`、`可以／不在`→`●／Ｘ`，並加一行圖例。
   3. 拿掉欄與欄之間的直線，只留外框和標題欄的分隔線（例：`│上│●●●●ＸＸ│`）。
   4. 完全不畫框，只靠全形字對齊。
   5. 還是超過 → 不做表格，改成條列，並跟用戶說明原因（例如格子要放地址、長句）。
4. **檢查**：把表格寫進暫存檔，執行 `python scripts/check_table.py <檔案>`，**通過才交付**。檢查會抓半形字、超寬、欄線對不齊、未實測符號。自己數格子很容易數錯，一律交給程式。
5. **交付**：表格放進 code block 方便複製，下面附圖例。提醒一句：在這個對話介面裡看起來可能是歪的，以 LINE 上顯示為準。

## 範本：空檔時間表（✅ 已在 LINE 實測）

用戶偏好的格式：直式（日期當列）、第一欄月份與日期都用兩位全形數字、`●`＝可以、`Ｘ`＝不在、時段只有上／下。

```
┌───┬─┬─┐
│０９月│上│下│
├───┼─┼─┤
│１５二│●│●│
│１６三│●│●│
│１７四│●│Ｘ│
│１８五│●│●│
│１９六│Ｘ│Ｘ│
│２０日│Ｘ│Ｘ│
└───┴─┴─┘
●＝可以　Ｘ＝不在
```

橫式（星期當欄）也實測成功過，但放不下日期，只適合不寫日期的情況：

```
┌─┬──────┐
│　│二三四五六日│
├─┼──────┤
│上│●●●●ＸＸ│
│下│●●ＸＸＸＸ│
└─┴──────┘
```

## 範本：報價表（✅ 已在 LINE 實測，寬 12 格）

單位放標題（`價格／元`），格子裡只放數字，不加千分位逗號，這樣省欄寬，也不會有半形字。

```
┌──┬────┬──┐
│方案│價格／元│工期│
├──┼────┼──┤
│基本│３０００│３天│
│標準│５０００│５天│
└──┴────┴──┘
```

## 停止條件

- 有日期但無法判斷是哪一週，而且用戶語意明顯不是「這週」→ 先問。
- 縮減到第 3.5 步還是放不下 → 改條列，不硬畫。

## 驗收標準

- `check_table.py` 通過（exit 0，沒有錯誤）。
- 日期與星期用指令驗過。
- 用戶說的每一個時段／數值都有對到格子，沒有漏或多。
- **範本以外的新表格類型**，第一次使用請用戶傳到 LINE 後截圖回報；確認沒歪，再把這個類型補進本 Skill 當範本。
